DROP TABLE IF EXISTS ORDERR;
DROP TABLE IF EXISTS INSTRUMENT;

create table ORDERR(
  ID  BIGINT not null PRIMARY KEY,
  PRICE DECIMAL not null,
  VOLUME   INTEGER  not null,
  TNUMBER   INTEGER  not null
);

CREATE  TABLE INSTRUMENT(
  ID INTEGER NOT NULL PRIMARY KEY,
  ORDER_ID INT NOT NULL REFERENCES ORDERR(ID),
  STATUS_BLOCKING INTEGER NOT NULL
);

insert into orderr  values (1, 1, 1000, 2);
insert into orderr values (2, 1, 10001, 2);


insert into INSTRUMENT  values (1, 1, 1);
insert into INSTRUMENT  values (2, 2, 2);

SELECT * FROM ORDERR;
SELECT * FROM INSTRUMENT;

SELECT PRICE FROM INSTRUMENT AS I, ORDERR AS O WHERE I.ORDER_ID = O.ID;